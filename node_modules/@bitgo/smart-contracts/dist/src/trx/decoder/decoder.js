"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TrxDecoder = void 0;
const ethereumjs_abi_1 = require("ethereumjs-abi");
const ethereumjs_util_1 = require("ethereumjs-util");
const types_1 = require("./types");
const ensure_1 = require("../../util/ensure");
const contractInstances_1 = require("../../base/contracts/contractInstances");
const contracts_1 = require("../contracts/contracts");
const string_1 = require("../../../src/util/string");
// TronWeb does not use ES Modules so we must use require
const TronWeb = require('tronweb');
class TrxDecoder {
    constructor() {
        this.methodsById = this.loadMethods();
    }
    loadMethods() {
        const result = {};
        for (const contractName of (0, contractInstances_1.listContractTypes)(contracts_1.TrxContract.chainName)) {
            const jsonTronAbi = (0, contractInstances_1.getAbiContract)(contractName, contracts_1.TrxContract.chainName, contracts_1.TrxContract.ACCESS_ABI_VALUES);
            for (const methodAbi of jsonTronAbi) {
                if (methodAbi.type === 'Function') {
                    const name = methodAbi.name;
                    methodAbi.inputs = methodAbi.inputs ? methodAbi.inputs : [];
                    const types = methodAbi.inputs.map((input) => input.type);
                    const methodIdString = (0, ethereumjs_util_1.bufferToHex)((0, ethereumjs_abi_1.methodID)(name, types));
                    result[methodIdString] = { contractName, abi: methodAbi };
                }
            }
        }
        return result;
    }
    decode(data) {
        if (typeof data === 'string') {
            data = (0, string_1.parseToBuffer)(data);
        }
        const methodId = (0, ethereumjs_util_1.bufferToHex)(data.slice(0, 4));
        (0, ensure_1.ensure)(this.methodsById[methodId], `Unknown method: ${methodId}`);
        const { contractName, abi: { name, inputs } } = this.methodsById[methodId];
        const names = inputs.map(({ name }) => name).filter(name => !!name);
        const types = inputs.map(({ type }) => type);
        const abiEncodedArgs = (0, ethereumjs_util_1.bufferToHex)(data);
        const decodedArguments = TronWeb.utils.abi.decodeParams(names, types, abiEncodedArgs, true);
        const args = [];
        for (let i = 0; i < inputs.length; i++) {
            const { name, type } = inputs[i];
            const decodedArgument = decodedArguments[name];
            args.push({ name, type, value: (0, types_1.formatValue)(decodedArgument, type) });
        }
        return {
            methodId,
            name,
            args,
            contractName,
        };
    }
}
exports.TrxDecoder = TrxDecoder;
//# sourceMappingURL=decoder.js.map