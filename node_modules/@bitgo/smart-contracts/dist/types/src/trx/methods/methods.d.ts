import { Method, MethodDefinition, Parameter } from '../../base/methods/methods';
export interface TrxMethodDefinition extends MethodDefinition {
    constant?: boolean;
    payable?: boolean;
    inputs?: Parameter[];
    outputs?: Parameter[];
}
export declare class TrxMethod implements Method {
    private _id;
    constant?: boolean;
    payable?: boolean;
    inputs: Parameter[];
    outputs?: Parameter[];
    name: string;
    type: string;
    get id(): string;
    constructor(definitions: {
        [key: string]: any;
    });
    call(params: {
        [key: string]: any;
    }): any;
    explain(): TrxMethodDefinition;
}
